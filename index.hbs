{{!< default}}
<div class="">
    <div class="">

        <div class="">
        {{#foreach posts}}

            {{> "card" page=../pagination.page }} {{!-- partials/card.hbs --}}

        {{/foreach}}
        </div>

        {{!-- If JS isn't enabled, fallback to standard pagination --}}
        <noscript>
            <div class="">
                {{pagination}}
            </div>
        </noscript>
    </div>
</div>
