{{!< default}}
<div class="max-w-6xl my-4">
    <div class="grid grid-cols-4 gap-4">
        <div class="col-span-3 ">
        {{#foreach posts}}
            {{> "card" page=../pagination.page }}
        {{/foreach}}
        </div>
        {{> "about" }}
        {{!-- If JS isn't enabled, fallback to standard pagination --}}
        <noscript>
            <div class="">
                {{pagination}}
            </div>
        </noscript>
    </div>
</div>
